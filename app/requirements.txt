# Streamlit Campaign Pacing Monitor: pinned dependencies
# Update policy: check monthly for CVEs, pin exact versions always.
streamlit==1.54.0
psycopg2-binary==2.9.10
pandas==2.2.3
numpy==2.2.3
plotly==6.0.1
