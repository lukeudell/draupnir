# Streamlit Campaign Pacing Monitor: Secrets
# Copy to secrets.toml and fill in real values. NEVER commit secrets.toml to git.
# In Docker, these values come from environment variables instead.

[connections.postgresql]
host = "localhost"
port = 5432
user = "portfolio_reader"
password = "CHANGE_ME"
dbname = "draupnir"
